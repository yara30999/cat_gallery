import 'package:flutter/material.dart';

class BottomNavIconEntity {
  final String label;
  final IconData icon;

  const BottomNavIconEntity({
    required this.label,
    required this.icon,
  });
}
